<h1>Tweets</h1>

<table>
  <tr>
    <th>Tweet</th>
  </tr>
<% @posts.each do |post| %>
  <tr>
    <td><%= post.content %></td>
    <td><%= link_to "Edit", edit_post_path(post.id)%></td>
  </tr>
<%end%>

</table>  

<td><%= link_to "+🕊", new_post_path %></td>